# SearchAIO 产品与架构约束

## 产品边界

SearchAIO 是一个尊重隐私、可静态部署的搜索路由工具。默认体验必须始终满足：

1. 一个页面将同一条查询路由到多个网页搜索与 AI 渠道。
2. 选择引擎后，本地搜索舞台应呈现该引擎的视觉语言，同时始终保留 SearchAIO 身份，不能伪装成目标服务。
3. “我帮你搜索”链接必须复用同一个已选搜索舞台，完成输入动画后才跳转。这是核心功能，不是可选装饰。

页面不得在用户提交前加载外部搜索结果、发送查询或上传搜索历史。允许仅保存浏览器本地偏好；如要保存搜索历史，必须另行设计明确的用户选择机制。

## 源码布局

```text
SearchAIO.html          应用源文件；部署时将其改名为 index.html
index.html              本地开发兼容入口；正式部署以 SearchAIO.html 改名为准
styles.css              共用响应式样式与引擎视觉配置
src/config/engines.js   声明式引擎注册表及注册表校验
src/i18n/               显示文本
src/services/           偏好设置与 URL 路由
src/features/           仅包含教学动画行为
src/main.js             状态、渲染与一次性事件绑定
test/                   无依赖的 Node 测试文件
```

## 搜索引擎注册表规则

- 每个引擎必须有稳定的 `id`、至少一个别名、至少一个 `!bang`，并声明可执行的浏览器路由：包含 `%s` 的 URL 模板，或明确的 `formSubmission`（`GET`/`POST`、`action` 与字段）。`prompt` 引擎可仅声明入口页。
- 路由必须唯一。发布前运行 `npm test`，或打开 `SearchAIO.html?selftest=1`。
- 修改第三方 URL 前，必须在正常浏览器会话中人工验证；自动 HTTP 探测可能受到登录墙或反爬机制影响。
- 首屏只使用本地资源；不能把第三方资源主机作为应用启动依赖。

## AI 路由模式

引擎注册表通过 `mode` 明确描述交互，不把所有 AI 服务伪装成同一种“可直接搜索”的服务：

- `search`：普通网页或垂直搜索，优先使用 URL 模板直接跳转。
- `ai-search`：尝试查询直达的 AI 搜索。例如 `Google AI Mode` 使用 `https://www.google.com/search?udm=50&q=%s`；该参数是 URL 适配层，不是 Google 的永久公开 API，并可能因地区、账号或重定向而回退到普通搜索，需定期人工复查。
- `prompt`：目标服务没有稳定、公开的查询直达形式。SearchAIO 复制用户提示词后打开新对话，界面必须明示“复制提示词”。

### 提交机制

静态页面仅使用浏览器原生导航：优先 `GET` URL；若第三方明确提供不依赖登录态、CSRF token 或私有接口的 HTML 表单端点，可在注册表中使用 `formSubmission` 以 `GET` 或 `POST` 提交。不得通过跨域 `fetch`、逆向私有聊天接口或伪造会话请求来代发 AI 消息。

现代 AI 对话通常需要账户会话、动态 token 与应用内部请求，因此不能把页面内点击“发送”误建模为一个通用的外部 `POST` 接口。没有经过持续验证的公开路由时，必须降级为复制提示词并打开对话页。

自动 AI 路由默认关闭。若后续增加“自动”开关，其优先级必须为：显式 `prefix:`/`!bang` > 用户手动选择 > 自动规则。自动规则只能在高置信度的探索、比较、解释或多步骤问题上建议 AI Mode，且必须在提交前显示目的地，不得静默改变用户的普通搜索目的地。

## 扩展准则

- 保持零运行时依赖，并可部署到任意静态主机。直接双击 `SearchAIO.html` 时使用生成的 standalone runtime；HTTP 部署使用 ES Modules。
- 修改 `src/*.js` 后运行 `npm run build:static`，再把 `SearchAIO.html` 改名为 `index.html` 发布。
- 全局事件只能在 `src/main.js` 中绑定一次；渲染函数仅能为新建的短生命周期按钮绑定事件。
- 对用户控制的内容使用 DOM API 和 `textContent`；不可将查询插入 `innerHTML`。
- 每个新功能都应具备可访问的控件、键盘路径和移动端布局。
